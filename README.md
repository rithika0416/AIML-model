# AIML-model
This project trains and evaluates Linear Regression, Logistic Regression, Decision Tree, and Random Forest models on a dataset. The data is split into training and testing sets, and performance is measured using accuracy, precision, recall, and F1-score to identify the best model.
